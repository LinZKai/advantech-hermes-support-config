.env
.env.*
*.key
*.pem
config.yaml
__pycache__/
*.pyc
*.log

# Retrieval log. Contains knowledge-base document content and must not be
# committed. The logs/ directory is what switches logging on, so it stays
# local to whoever is reviewing.
*.jsonl
foundry_iq_retrievals.jsonl
logs/